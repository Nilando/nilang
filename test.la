lexer = { 
  parse_line: fn() {
    line = input();

    i = 0;
    while i != line.len {
      if line[i] == "+" {
        return "+";
      } else {
        return "Unknown Token";
      }

      i = i + 1;
    }
  },
  current_line: "",
};

msg = "hello world!";
chars = msg.to_list();
blah = ads + asdf - asdf / asdf * asdfasdf == "testset" + ( true == false );

dumb_fn = fn (a, b) {
  while a == b {
    if blah == ( true  + 69) {
      if true { 
        blah = ads + asdf - asdf / asdf * asdfasdf == "testset" + ( true == false );
        return 2;
      } else {
        blah = ads + asdf - asdf / asdf * asdfasdf == "testset" + ( true == false );
        return 1;
      }
    }
    a = 0;
    return "test";
  }
};
