VERSION 0.1.0
- display syntax and runtime errors nicely
- custom bytecode format
- functions are first class citizens
- function closures
- global variables
- simple read & print io
- working repl
- concurrent garbage collection
- standard library
- lsp
- interop with rust
- import and export files
- async and await
- type type
- error throwing and catching
- dependency manager
- basic static analysis
  - dead code warnings
  - unused variable warnings
  - unused expression warnings

BUGS:
- only allow one of each sym in func input

fn create_user(params) {
  this = params;

  this.get_name = fn() {
    return this.name;
  }

  this.set_name = fn(name) {
    this.name = name;
  }

  this.say_name = fn() {
    print this.name;
  }
  
  this.is_confirmed = fn() {
    return this.name;
  }
}
