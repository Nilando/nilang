[package]
name = "gpu_plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
nilang = { path = "../.." }
sandpit = { version = "=0.5.3", features=["multi_threaded"] }
minifb = "0.27"
